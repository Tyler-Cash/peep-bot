## 2026-03-20 - Character counter for large text inputs
**Learning:** For fields with large character limits (like 3800), users often lose track of how much they've written. A real-time character counter provides immediate feedback and prevents frustration on form submission.
**Action:** Always include an accessible character counter (`aria-live="polite"`, `aria-describedby`) for textarea fields with significant length constraints.
